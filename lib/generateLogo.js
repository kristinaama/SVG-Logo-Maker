const shapes = require('shapes.js');

// Create a function that creates a logo based off of user input, and writes it onto an SVG file
function createLogo(file, results) {
    let userLogo = "";
    userLogo = '<svg version="1.1" width="300" height="200" xmlns="http://www.w3.org/2000/svg">';
    
};

module.exports() = {
    createLogo
};